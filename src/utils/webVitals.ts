// Web Vitals tracking utilities - DISABLED
export const trackWebVitals = () => {
  // Performance tracking disabled
};

export const trackResourceTiming = () => {
  // Resource timing tracking disabled
};

declare global {
  function gtag(...args: any[]): void;
}